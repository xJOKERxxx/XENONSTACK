const students = [
    { name: "Ankush", city: "Dehradun" },
    { name: "Arpit", city: "Dehli" },
    { name: "Nick", city: "Mumbai" }
]

for (const student of students) {
    console.log(`${student.name} lives in ${student.city}`);
}