function adressMaker(city, state) {
    //const newAdress={newCity:city ,newState:state};
    const newAdress = { city, state }; //in object literals we dont have write duplicate code.
    console.log(newAdress);
}
adressMaker('Dehradun', 'UK');